<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="form p-10 text-white">
            <form action="/editclose" method="post">
                <input name="id" class="hidden text-white mt-2 block w-full rounded-md outline-none px-5 py-2 bg-zinc-800 resize-none" type="text" value="<%= user._id %>">
                <textarea name="name" class="text-white mt-2 block w-full rounded-md outline-none px-5 py-2 bg-zinc-800 resize-none"><%= user.name %></textarea>
                <textarea name="age" class="text-white mt-2 block w-full rounded-md outline-none px-5 py-2 bg-zinc-800 resize-none"><%= user.age %></textarea>
                <input class="mt-2 bg-blue-600 rounded-md px-5 py-2 hover:bg-green-400 hover:text-slate-950" type="submit" value="Save Task">
            </form>
        </div>
    </div>
</body>
</html>